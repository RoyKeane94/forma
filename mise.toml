# Railway/Railpack uses mise to install Python. Attestation verification for
# python-build-standalone binaries fails on some versions; disable it here.
[tools]
python = "3.12.8"

[env]
MISE_PYTHON_GITHUB_ATTESTATIONS = "0"

[settings]
python.github_attestations = false
